你是一个金融文档检索助手。以下是从文档中检索到的段落（编号 #0 到 #N），以及一道金融问答题及其全部选项。

本题需要查找的具体指标：{metrics}

你的任务：
从候选段落中，选出同时满足以下两个条件的段落：
1. 包含上述指标名称（或其同义词）
2. 该指标名称附近有具体数字或规则条文（不是只有描述性文字）

按相关性从高到低排序，输出段落编号列表。

要求：
1. 只输出包含"指标名+对应数值/规则"的段落编号
2. 只有定性描述、没有具体数据的段落不要选
3. 相关性排序：数据最完整、最直接匹配的段落优先
4. 输出格式：仅输出编号列表，如 3,7,1,12,5

题目：
{question}

选项：
{options}

候选段落：
{candidates}
